<!DOCTYPE html>
<html>
    <head>
        <title>Reservation Verification</title>
    </head>
    <body>
        <h1>Verify your reservation</h1>
        <p>
            <a href="{{ url }}">Click here to verify your reservation</a>
            (this link is valid until {{ expiration_date|date('F jS \\a\\t H:i') }})
        </p>
        <h2>Reservation Info</h2>
        <p>Organization Name: {{ organization_name }}</p>
        <p>Service Name: {{ service_name }}</p>
        <p>Date and Time: {{ start_date_time }}</p>
        <p>Estimated price: {{ estimated_price }}</p>
        <p>Duration: {{ duration }}</p>
    </body>
</html>
